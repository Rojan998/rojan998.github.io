import type { Certification } from "./types";

/**
 * Certifications actually earned. The Certifications section is hidden
 * automatically (see app/page.tsx) whenever this array is empty — do not
 * add entries that have not been earned.
 */
// Add solutions architect aws
export const certifications: Certification[] = [];
